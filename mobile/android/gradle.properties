org.gradle.jvmargs=-Xmx4G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# Gradle must run on JDK 21+ (Flutter plugin uses Java 21). App compiles with Java 17 (see app/build.gradle).
# If build fails with "Unsupported class file major version 65", set JAVA_HOME to JDK 21 or add:
# org.gradle.java.home=/path/to/jdk-21
